import { UnprocessableEntityErrorFilter } from './unprocessable-entity-error.filter'

describe('UnprocessableEntityErrorFilter', () => {
  it('should be defined', () => {
    expect(new UnprocessableEntityErrorFilter()).toBeDefined()
  })
})
